# 🔴 Weak Register — Sổ câu còn yếu (ưu tiên hỏi lại)

> Danh sách câu đã trả lời **sai / chưa chuẩn / lúng túng** (điểm ≤ 2 theo [config.md §4](config.md)) — interviewer **ưu tiên chèn lại** vào các phiên. Không có luật "đúng rồi thôi": câu ở đây được hỏi lại tới khi vững (≥ 3 điểm **hai lần liên tiếp** → gỡ khỏi sổ).
> Interviewer cập nhật file này ở Bước 4 mỗi phiên. Git-track để thấy điểm yếu co lại dần.

## Cách đọc
- **Lần gặp** = số phiên đã hỏi câu này. **Điểm gần nhất** = điểm 2 lần mới nhất (vd `2, 3`).
- Sắp xếp: câu điểm thấp + gặp nhiều lần mà chưa lên → **ưu tiên cao nhất**.

## Bảng câu yếu

> 🎭 **Cụm `BEH-*` (7 câu, thêm 2026-08-23) chung ĐÚNG MỘT nguyên nhân gốc** — mọi câu trả lời dừng ở chữ **A** của STAR, **0/12 câu tự đi tới R**, **0 con số tự nguyện nói ra**. Phân tích đầy đủ đặt **một chỗ** ở [khối *NGUYÊN NHÂN GỐC* trong log 23/08](sessions/2026-08-23--rapid--behavioral.md); mỗi dòng dưới chỉ ghi **phần riêng** của câu đó. *(Chép phân tích chung vào cả 7 dòng chính là lỗi "một sự thật, nhiều chỗ" mà repo đã dính 4 lần.)* ✅ **ĐÃ CHỮA — đo lại 2026-08-24: 2.42 → 3.42 (+1.00), 0/12 câu cần probe** (trước: 6/12), **5/5 câu STAR có R tự nguyện** (trước: 0/12), **≥6 con số tự nguyện** (trước: 0). Cả 7 câu nay **1/2 lần ≥3** — cần **một lần ≥3 nữa** mỗi câu để gỡ. ⚠️ **Trần mới lộ ra sau khi lỗi cũ đóng** (xem [log 24/08](sessions/2026-08-24--rapid--behavioral.md)): **dừng ở "sửa xong", chưa tới "ngăn tái diễn"** — BEH-004 nêu *phát hiện* thay vì *phòng ngừa*; BEH-010 dừng ở hotfix, thiếu **containment** + **post-mortem**. Đây là khoảng cách **mid → senior**.

| ID | Câu (tóm tắt) | Track | Lần gặp | Điểm gần nhất | Ghi chú lỗ hổng |
|----|---------------|-------|---------|----------------|-----------------|
| BEH-006 | bất đồng quan điểm — kể MỘT lần cụ thể | behavioral | 2 | 1, **4** | 🔼🔼 **1/2 lần ≥3 — bước nhảy lớn nhất** (2026-08-24, góc *quyết định kỹ thuật bị phản đối*, [log 24/08](sessions/2026-08-24--rapid--behavioral.md)). ✅ **Đã có đúng mắt xích bank gọi là phần được chấm — ĐƯA DỮ LIỆU**: ca `insmod` FRC/TCON 500ms→3-4s · `call_usermodehelper()` spawn ở priority mặc định 120 · hai giả thuyết cạnh tranh (multithread vs `SCHED_FIFO`) · **test 10 lần, 7/10 đúng giờ** · áp cả hai · **100 lần boot, không còn delay >500ms**. ⭐ Còn **tự kiểm tra tác động hệ thống** (request team Performance) trước khi áp `SCHED_FIFO` — tín hiệu senior. ❌ Còn sót: **bỏ trống số liệu phương án của đồng nghiệp** (người nghe đang chờ so sánh); và dài ~150s, vượt trần `rapid` — nhưng dài **vì có nội dung**. **Lần sau hỏi:** *"nếu team Performance nói `SCHED_FIFO` gây rủi ro toàn hệ thống thì em còn phương án nào?"* — ép nghĩ tiếp khi giải pháp tốt nhất bị chặn. Một lần ≥3 nữa là gỡ |
| BEH-001 | thành tựu tự hào nhất | behavioral | 2 | 2, **3** | 🔼 **1/2 lần ≥3** (2026-08-24, góc *phần nào KHÓ nhất*, [log 24/08](sessions/2026-08-24--rapid--behavioral.md)). ✅ **Chi tiết bị bỏ quên 2 phiên nay đã tự xuất hiện**: *"chỉ có 1 binary duy nhất"* — đúng thứ log 18/08 kê đơn. **R tự nguyện**: *"2 tháng, pass QA, software xuất xưởng"*. **Không cần probe.** ❌ Chưa 4 vì **dài ~110s và có đoạn thừa** — khung *"nguyên nhân chủ quan / khách quan"* hợp cho [BEH-010](bank/behavioral.md), **lạc chỗ** ở câu *"phần nào khó nhất"*; người nghe chờ *"khó vì đâu"*. Bản rút gọn ~50s có sẵn trong log. **Lần sau hỏi:** *"nếu làm lại từ đầu, em sẽ làm khác chỗ nào?"* |
| BEH-002 | bug khó nhất | behavioral | 2 | 2, **4** | 🔼 **1/2 lần ≥3** (2026-08-24, góc *bug chẩn đoán SAI hướng lúc đầu*, [log 24/08](sessions/2026-08-24--rapid--behavioral.md)). ✅ Đúng khung: **giả thuyết sai → bác bỏ bằng bằng chứng → xoay hướng → nguyên nhân thật**. Nêu thẳng giả thuyết sai (*video source*) rồi bác bỏ bằng số đo (*1920x1080@60Hz bình thường*). ⭐ Đi xa hơn phiên 18/08 **một tầng**: gốc không dừng ở *"thiếu unmute"* mà truy tới **thiếu sót lúc porting FRC driver từ base driver** — trả lời được *"vì sao lỗi TỒN TẠI"*. R có, không cần probe. ❌ Còn thiếu: **không gọi tên KỸ THUẬT** — đã làm đúng *"đặt đường tắt và đường bật cạnh nhau"* nhưng không phát biểu thành câu. Kể sự cố ⇒ họ tin bạn sửa được MỘT bug; gọi tên kỹ thuật ⇒ họ tin bạn sửa được CẢ LỚP bug. **Lần sau hỏi:** *"em có bao giờ sửa một bug rồi sau đó phát hiện mình sửa nhầm chỗ không?"* |
| BEH-005 | điểm yếu của bạn | behavioral | 2 | 2, **3** | 🔼 **1/2 lần ≥3** (2026-08-24, góc *3 tháng nâng MỘT kỹ năng*, [log 24/08](sessions/2026-08-24--rapid--behavioral.md)). ✅ Chọn **System Design**, mạch lạc, có phương pháp cụ thể (design pattern · đề tài thực tế · chọn SoC/MCU · phân tích đánh đổi) và **đã bắt đầu**, không phải dự định. ❌ Hai điểm: **①** **không chọn lỗ hổng chính mình đã khai** — phiên trước nói điểm yếu là **debug tooling**, [CLAUDE.md §2](../../CLAUDE.md) ghi vậy, và đang nợ **7 bài 🧪 lab DBG-030…036** nhắm đúng đó ⇒ interviewer ghi *"biết mình thiếu gì nhưng chưa ưu tiên vá"*. **②** ⚠️ **MỘT CÂU CÓ RỦI RO THẬT, nên bỏ khỏi vốn từ**: *"viết code, ngôn ngữ lập trình, testing hoàn toàn có thể làm tốt bởi AI"* — nói với người đang tuyển kỹ sư C++ nhúng (và có thể chính họ viết code đó hằng ngày). Đổi thành: *"AI giúp em đi nhanh ở phần lặp lại, nên phần em muốn đầu tư là chỗ nó không làm thay được — chọn kiến trúc, xác định ràng buộc, cân đánh đổi."* **Lần sau hỏi:** *"trong 3 tháng đó em đo tiến bộ bằng gì?"* — ép ra tiêu chí nghiệm thu |
| BEH-011 | học thứ mới trong thời gian ngắn | behavioral | 2 | 2, **3** | 🔼 **1/2 lần ≥3** (2026-08-24, góc *hỏi thẳng "em có làm Yocto bao giờ chưa?"*, [log 24/08](sessions/2026-08-24--rapid--behavioral.md)). ✅ **Đã chọn ĐÚNG chất liệu** (phiên trước chọn Windows App — dòng ít liên quan JD nhất). Cấu trúc chuẩn: thừa nhận **một mệnh đề rồi đi tiếp ngay** → lý do → bằng chứng (BBB, bitbake/OE-core/poky) → **kết quả kiểm chứng được** (*boot lên, shell hoạt động*). ~35s, gọn. ❌ Chưa 4 vì **thiếu đúng phần bank chấm — BẮC CẦU**: trả lời như thể Yocto là *sở thích rời* với công việc, trong khi nó nối thẳng vào **cross-compilation + CMake/Makefile đa nền tảng + porting driver qua nhiều chipset** đang làm hằng ngày. Thêm một câu là thành 4: *"nền thì em có sẵn… nên vấn đề Yocto giải quyết — dựng một image tái lập được cho nhiều board — em hiểu bài toán."* **Lần sau hỏi:** ép sang mảng **trống hẳn** — *"còn PCI/USB thì sao?"* — xem có giữ được cấu trúc *thừa nhận → bắc cầu* khi **không có bằng chứng** để đưa |
| BEH-007 | vì sao nên tuyển bạn | behavioral | 2 | 2, **3** | 🔼 **1/2 lần ≥3** (2026-08-24, góc *nêu 3 thứ + XẾP HẠNG thứ nào đội thiếu nhất*, [log 24/08](sessions/2026-08-24--rapid--behavioral.md)). ✅ **Ba trụ, mỗi trụ có bằng chứng** (phiên trước: *"trải dài nhiều tầng"* không bằng chứng nào): HAL xuyên tầng trên sản phẩm thật · bring-up/porting + device tree + đọc board info ở U-Boot + truyền DTS args · tự động hoá **có con số tự nguyện**. ❌ Chưa 4 vì **lập luận xếp hạng vòng tròn**: *"JD nhấn mạnh X"* ⇒ *"đội thiếu X"* — JD nhấn thứ **quan trọng**, không nhất thiết thứ **khó tuyển**. Bản sắc hơn tách hai điều đó (có trong log). ⚠️ **Con số trôi qua 3 phiên bản**: resume *>50%* · 23/08 *1 ngày→10 phút+1 tiếng* · 24/08 *1-2 ngày→1-2 giờ*. Cùng bậc nên không bị bắt bẻ, nhưng **chốt MỘT phiên bản và dùng mãi** — interviewer có ghi chép. **Lần sau hỏi:** *"nếu đội đã có người mạnh cả ba thứ đó thì em còn mang lại gì?"* |
| BEH-009 | câu hỏi ngược interviewer | behavioral | 2 | 2, **4** | 🔼 **1/2 lần ≥3** (2026-08-24, góc *yêu cầu BA câu*, [log 24/08](sessions/2026-08-24--rapid--behavioral.md)). ✅ Ba câu, **cả ba đều kỹ thuật**, trúng cả ba mục bank: ① SoC + platform (Yocto/Buildroot/full distro) ② tỉ lệ userspace vs kernel space ③ hạ tầng debug cross-layer — *có debug hardware không, gdb/valgrind trên thiết bị thật hay chủ yếu log/tracing/coredump*. ⭐ Câu ③ hay nhất: cho thấy hiểu debug trên embedded là **bài toán hạ tầng**, không phải kỹ năng cá nhân, và ngầm nói biết chạy `valgrind` trên target là **không đương nhiên** — **vừa hỏi vừa chứng minh**. ❌ Còn thiếu: chưa hỏi **CI / build pipeline / cách test trên phần cứng thật**, và **kỳ vọng sau 6 tháng**. **Lần sau hỏi:** yêu cầu **5 câu**, xem có chạm CI và lộ trình vai trò không |
| DP-002 | Singleton trong shared library — còn *một* instance không? | cpp-system, design-patterns | 3 | 4, **1** | 🔻 **REGRESSION 4 → 1** (2026-08-21, góc *hai `.so` cùng include header Singleton*). Gỡ khỏi sổ 07/08 ở mức 4, retention 2 tuần sau **rơi thẳng**. ❌ Kết luận T1 **đảo ngược**: trả lời **2 instance**, đáp án là **1** (chạy thật: cùng địa chỉ `0x7b465ec6c03c`, count 1→2→3). 🔴 **Lỗi nặng nhất — chẩn đoán SAI TẦNG vấn đề:** đề xuất **shared memory** cho hai `.so` **cùng một tiến trình, cùng một không gian địa chỉ**; shared memory là cơ chế **liên tiến trình**, ở đây không giải quyết gì. Nghe *"hai `.so`"* là nhảy sang khung *"hai bên tách biệt"*. 🟡 **Cơ chế CÓ trong đầu nhưng bỏ:** follow-up (d) nói *"chưa rõ, theo tôi nó sẽ chọn một và duy trì một cái"* — **chính là symbol interposition**, nhưng phát biểu dạng phỏng đoán và **mâu thuẫn với (a)** mà không nhận ra. ⚠️ Mẫu lặp **lần 3** (LNX-026 15/08 · BSP-029 19/08 · DP-002 nay). Cần nắm: tính duy nhất **do dynamic linker bảo đảm, KHÔNG do chuẩn C++**; ba thứ lật ngược = `-fvisibility=hidden` · `dlopen(RTLD_LOCAL)` · link tĩnh (chứng minh chạy thật: hidden ⇒ 2 địa chỉ khác nhau, count riêng). ⚖️ **Repo có lỗi thật (lần 4):** bank DP-002 là 🟢 3 dòng, `creational.md` không nhắc `.so`, cơ chế nằm ở [linking-loading.md:100](../../07-shared-libraries/linking-loading.md#L100) đúng **một dòng** không nối sang. **Đã vá 21/08**: thêm [DP-020](bank/design-patterns.md) + cross-link + cảnh báo ở `creational.md §1`. **Lần sau hỏi:** ⚠️ **T1 trước** ([config §6](config.md) cấm đưa câu ≤2 lên T2) — *"cùng một binary, cùng một tiến trình, vì sao `-fvisibility=hidden` lại đổi được SỐ LƯỢNG object trong chương trình?"* Kiểm đúng chỗ hụt: **AI** quyết định tính duy nhất |
| CPP-016 | object slicing — chặn từ phía class | cpp-system, modern-cpp | 2 | 2, **3** | 🔼 **1/2 lần ≥3** (2026-08-21, góc *`Device`/`Panel` — chặn từ phía class, không sửa call site*). ✅ **Tầng thiết kế đã mở** — đúng thứ trắng lần trước: nêu được **hai** cơ chế kèm đánh đổi (`= delete` copy ⇒ `Panel` **cũng** mất copy · base **abstract** ⇒ mất hành vi mặc định), và giải thích **rất chính xác** vì sao `name` sống mà đa hình chết (*vptr không được copy sang*). ❌ Còn hụt ở follow-up (d): đề xuất *"cấm copy ở `Device`, tự viết copy ở `Panel`"* — **compile được** (đã thử, exit 0) nhưng copy ctor của `Panel` không gọi nổi copy ctor đã `delete` của base ⇒ buộc dựng base bằng default ctor ⇒ **dữ liệu `Device` im lặng không được copy**: đổi *slicing lộ liễu* lấy *mất dữ liệu âm thầm*. Chưa nêu **`protected` copy ctor** (bank CPP-016 bẫy (3) có sẵn) — cách duy nhất giữ được copy `Panel`→`Panel` mà vẫn chặn cắt. ⭐ **Ý chưa nắm, đáng nhớ nhất:** ba cách bắt lỗi ở **hai thời điểm khác nhau** — `protected`/`= delete` chỉ nổ **khi GỌI**; chỉ **abstract base** nổ **ngay tại KHAI BÁO** `void shutdown(Device d)` (*chứng minh chạy thật: `error: cannot declare parameter 'd' to be of abstract type 'Device'`*). **Lần sau hỏi:** góc 3 — *"`std::vector<Device> v; v.push_back(panel);` compile sạch, chạy không crash, nhưng sai. Chuyện gì xảy ra, và `protected` copy ctor có cứu được ca này không?"* (ca container — bank gọi là ca nguy hiểm nhất). Chỉ cần **một** lần ≥3 nữa là gỡ |
| OS-020 | thread-safe vs reentrant / async-signal-safe | cpp-system, os | 2 | 2, **3** | 🔼 **1/2 lần ≥3** (2026-08-13 phiên 2, góc *daemon treo 3–4/500 lần `systemctl stop`*). ✅ **Nền đã vá xong**: gọi đúng `printf`/`free` là hàm cấm, dựng đúng cơ chế khoá heap, tự giải thích được vì sao lỗi hiếm. Hết nhầm *"`write()` cấm"* và *"`_r` = signal-safe"* của lần trước. ❌ Còn hụt ở tầng vận dụng: (1) **đề xuất `SA_RESTART` — quyết định NGƯỢC**, nó làm SIGTERM vô hiệu (đã chứng minh chạy thật: `sa_flags=0` thoát rc=0 · `SA_RESTART` treo, phải SIGKILL); (2) `printf` + `_exit` **mất log** vì `_exit` không flush stdio (stdout→journald là pipe = full-buffered) — chỉ nêu được sau khi bị thu hẹp đề; (3) không biết systemd chờ **`TimeoutStopSec` 90 s** rồi SIGKILL; (4) diễn đạt lệch: async-signal-safety là *"gọi được TỪ TRONG handler không"*, không liên quan tới `read()` chặn ở main loop. Sinh ra câu bank mới [LNX-030](bank/linux-sysprog.md). **Lần sau hỏi:** cho daemon dùng `epoll_wait` + `signalfd` → *"vì sao kiến trúc này làm cả `EINTR` lẫn cờ `volatile` biến mất?"*, so sánh với self-pipe trick. Chỉ cần **một** lần ≥3 nữa là gỡ |
| LNX-005 | short read — TCP là luồng byte, không ranh giới message | linux-sysprog, bsp, cpp-system | 2 | 2, **2** | ➡️ **Đứng yên** (2026-08-15, góc *delimiter `'\n'`, client gửi 1 MB không có `'\n'`*). ✅ **Đọc code chuẩn xác**: lần đúng `memchr` luôn `NULL`, `handle_message` không bao giờ chạy, `len` tăng tới đầy; biết `read(..., count=0)` **trả về 0**. ❌ **Nhưng (b) "sửa thế nào" TRẮNG HOÀN TOÀN** — đây là lỗi lặp lại với Q3/Q5/Q7 cùng phiên: *chẩn đoán được, sửa không được*. Ba thứ phải nắm: **①** hệ quả của `read(...,0)` = `n<=0` ⇒ `break` ⇒ kết nối đóng **im lặng, log trông y hệt client tự ngắt** (ứng viên tưởng ngược lại: "người vận hành sẽ nghĩ là có lỗi"); **②** cách sửa = **trần cho một message** (64 KB), đầy mà chưa thấy delimiter ⇒ protocol violation ⇒ log + đóng; 200 kết nối × buffer tự lớn 1 MB = 200 MB / 256 MB ⇒ **DoS rẻ tiền**; **③** length-prefix không xoá lớp lỗi mà đổi mặt: `len = 0xFFFFFFFF` ⇒ `malloc(4 GB)` ⇒ crash hoặc OOM killer — **so `len` với trần TRƯỚC khi cấp phát**, cộng bẫy endianness (`ntohl`). **Lần sau hỏi:** ép **VIẾT CODE** ([config §6 luật ⑤](config.md)) — *"viết `read_message(fd, buf, max)` xử lý đủ: short read, thiếu delimiter, vượt trần, `EINTR`, EOF"*. Đã chẩn đoán đúng hai lần rồi, lần ba phải ra được mã sửa. *(ghi chú cũ 2026-08-13:* Liệt kê **đủ và đúng cả 4 ca** của `n` (8 · 1–7 · 0 · −1) và biết `EINTR` không phải lỗi thật → T1 chắc. Nhưng **T2 TRẮNG**: hỏi *"vì sao lab (LAN) đúng mà hiện trường (Wi-Fi) sai"* chỉ trả lời được *"Wi-Fi không ổn định"* = hiện tượng; hỏi thẳng cơ chế TCP thì **"chưa rõ"**. Cần nắm: **TCP chỉ hứa đủ byte + đúng thứ tự, KHÔNG hứa đúng lô** — header 8 byte vắt qua 2 segment (MSS, Nagle+delayed ACK, retransmit); LAN RTT 0.1 ms nên luôn gọn một segment ⇒ race không bao giờ lộ ở lab. Hệ quả phải tự rút ra: **mọi protocol trên TCP phải tự framing** (length-prefix/delimiter) + `read_full()`. 📌 Kèm lỗ hổng tài liệu: repo chưa nói thẳng ý này ở đâu, chỉ nằm rải trong bảng TCP/UDP. Góc đã dùng: cho code đã có `read_full` đúng nhưng protocol dùng **delimiter `\n`** → *"gói 1 MB không có `\n` thì chuyện gì xảy ra?"* — **đã hỏi 15/08**, cấm lặp*) |
| LNX-026 | `mmap` vs `read`/`write` — page fault, `SIGBUS`, `msync` | linux-sysprog, bsp, cpp-system | 1 | **2** | 🆕 2026-08-15 (góc *file 300 MB quét tuần tự lúc boot, đồng nghiệp đổi sang `mmap`: không nhanh hơn + `SIGBUS` chỉ ở hiện trường*). 🔎 **Ca đặc biệt — cơ chế CÓ trong đầu nhưng phát biểu dưới dạng phỏng đoán**: hai lần mở đầu *"chưa rõ"* rồi nói ra thứ gần đúng (*"cần synchronize từng page 4KB khi động đến"* = **chính là page fault**; *"file bị chỉnh sửa lúc đọc"* = **chính là truncate dưới chân mapping**). Phải khoan 2 lượt mới ló. ❌ Thiếu thật: **readahead** (cơ chế bù trừ của `read()` — kernel nạp trước, I/O chồng lấn xử lý), **phép nhân ra con số** (300 MB / 4 KB = ~76.800 page fault vs 4.800 syscall), và tiêu chí chọn thiếu chữ **"ngẫu nhiên"** + **"chia sẻ giữa process"**. Ban đầu còn suy sai *"map không đủ lớn"* — không thể, thiếu địa chỉ thì lỗi ngay tại `mmap()`. **Ý đáng nhớ nhất chưa nói được:** `read()` báo lỗi **qua giá trị trả về** (xử lý được), `mmap` báo lỗi **qua signal** (mặc định chết) ⇒ đổi mô hình truy cập là đổi luôn mô hình báo lỗi. ⚖️ **Repo có lỗi thật:** LNX-026 trỏ `→ file-io.md` mà file đó nói về `mmap` đúng **1 dòng** — lần thứ **3** gặp mẫu này (sau LNX-023, LNX-029). Bank đã viết lại đầy đủ 2026-08-15. **Lần sau hỏi:** đảo chiều sang **ghi** — *"log ghi bằng `mmap`+`msync`, mất điện đột ngột thì mất gì so với `write`+`fsync`?"* (thứ tự trang không đảm bảo) |
| LNX-024 | namespaces & cgroups — nền tảng container | linux-sysprog, bsp | 2 | 0, **3** | 🔼 **1/2 lần ≥3** (2026-08-16, phiên `rapid`, hỏi thẳng khái niệm). ✅ Ra ngay cả hai vế: *"namespace = cô lập tầm nhìn (pid/ipc/mount), process ngoài không thấy không đụng được"* · *"cgroup = cô lập tài nguyên, process crash vì memory không lan sang giết process khác"*. **Đã đọc bản bank viết lại** — đúng việc §📍 giao làm khởi động. ❌ Chưa 4 vì cgroups chỉ mô tả qua **một** biểu hiện (OOM cục bộ); nó rộng hơn — **giới hạn + hạch toán**: `MemoryMax` · `CPUQuota` · io · `TasksMax` (chống fork bomb), và phần *hạch toán* (đo ai dùng bao nhiêu) quan trọng ngang phần giới hạn. ⚠️ **Đây mới là T1** — phiên `rapid` không hỏi T2, nên **chưa được coi là vững**; cần một lần ≥3 ở **tầng vận dụng** nữa mới gỡ. **Lần sau hỏi (T2, bắt buộc không hỏi lại định nghĩa):** *"đặt `MemoryMax=64M` cho app đối tác rồi mà daemon của bạn vẫn chết — nghi gì?"*. *(ghi chú gốc 2026-08-15:* ⚖️ **Lỗ hổng TÀI LIỆU trước, lỗ hổng ứng viên sau:** `grep` toàn repo → **không có tài liệu nào dạy namespaces/cgroups**, mà LNX-024 lại trỏ `→ ipc-linux.md` (file đó chỉ có một chữ "namespace", và là *abstract namespace của Unix socket* — chuyện khác hẳn). Câu vẫn hợp lệ vì nằm trong pool `LNX-013…026` của Buổi 2. Cần nắm — **hai cơ chế, hai câu hỏi khác nhau**: **namespace** = *"process này THẤY được gì"* (pid/mount/net/uts/ipc/user); **cgroup** = *"được DÙNG bao nhiêu"* (memory/cpu/io/pids). Ca kinh điển: app đối tác rò RAM ⇒ OOM killer giết nhầm daemon của mình ⇒ **cgroup memory limit** làm OOM thành **cục bộ**; `/tmp` đầy ⇒ **mount ns**; trùng cổng ⇒ **net ns**. Container = **ns + cgroup + rootfs riêng**, vẫn chung kernel. 🎯 Trên embedded gặp qua **systemd unit** (`MemoryMax=`, `PrivateTmp=yes`, `TasksMax=`), không phải Docker. Bank đã viết lại đầy đủ 2026-08-15. Góc đã dùng: T1 định nghĩa hai cơ chế — **đã hỏi 16/08, cấm lặp**.*) |
| LNX-040 | peer biến mất không FIN/RST — bao lâu thì biết | linux-sysprog, bsp, networking | 1 | **0** | 🆕 2026-08-15. **Trắng**; đoán `EPIPE` — **sai**, `EPIPE` cần một **RST bay về** mà ở đây không có gì bay về cả. ⚖️ **Lỗi của interviewer:** `grep -rn "keepalive\|ETIMEDOUT"` toàn repo → 0 kết quả liên quan; ra câu repo không đỡ được. Điểm ghi nhận nhưng **ưu tiên là viết tài liệu trước**, đừng hỏi lại khi chưa có chỗ ôn. Cần nắm: **①** `write()` đầu tiên **THÀNH CÔNG** — nó chỉ nghĩa *"đã chép vào buffer gửi của kernel"*, không bao giờ nghĩa *"bên kia đã nhận"*; **②** kernel retransmit thang gấp đôi rồi trả **`ETIMEDOUT`** sau **~15–20 phút**; **③** rút ngắn bằng 2 tầng — `SO_KEEPALIVE`+`TCP_KEEPIDLE/INTVL/CNT` (không sửa protocol, nhưng chỉ phát hiện **đường** chết) hoặc **heartbeat** ở tầng application (phải sửa protocol, đổi lại phát hiện được cả **app treo mà TCP vẫn sống**). Câu chốt: *"im lặng và khoẻ mạnh trông giống hệt nhau"*. **Lần sau hỏi:** ⚠️ **chỉ sau khi đã viết tài liệu** — khi đó vào thẳng T2: *"keepalive 5 s cho 5.000 kết nối trên 4G tính tiền theo byte — vấn đề gì?"* |
| SD-016 | bộ nhớ tất định — phân mảnh & pool | cpp-system, bsp | 2 | 2, **3** | 🔼 **1/2 lần ≥3** (2026-08-19, góc *vì sao malloc không tất định*). Lần 1 chỉ nêu được một nửa; lần 2 nêu **đủ cả vấn đề (phân mảnh) và giải pháp (pool cấp một lần)**. **Còn thiếu:** vế *tất định theo THỜI GIAN* — `malloc` không có chặn trên cho thời gian thực thi (duyệt/tách/gộp free list · `brk`/`mmap` + page fault · chờ lock allocator). **Lần sau hỏi:** T1, góc *"cùng một dòng `malloc`, vì sao lần 100 ns lần 100 µs?"* — nếu đạt ≥3 nữa thì gỡ. |
| LNX-029 | clock cho timeout — MONOTONIC vs REALTIME | linux-sysprog, bsp, cpp-system | 2 | 1, **2** | 🔼 **Nền đã vá xong** (2026-08-15, góc *`pthread_cond_timedwait` 5 s đúng lúc NTP nhảy*): nêu **cả hai chiều** (nhảy tiến ⇒ timeout ngay · nhảy lùi ⇒ chờ dôi) + biết phụ thuộc **clock của đối tượng chờ** — đúng thứ trắng lần trước. ❌ Nhưng theo [config §6 luật ①](config.md), câu weak **không chấm phần nền** — và toàn bộ follow-up hụt: (1) không biết **chỗ sửa** `pthread_condattr_setclock(&attr, CLOCK_MONOTONIC)` (mặc định condvar là REALTIME = **mặc định sai**); (2) hỏi cách **khớp** một dòng log giờ-thật với một phép đo đơn-điệu thì trả lời nhầm sang cách **lọc** log chậm >500 ms; (3) trắng ở câu *"NTP nhảy lần nữa lúc 3h sáng thì sao"*. Cần nắm: in **dòng NEO định kỳ chứa CẢ HAI mốc** (`realtime=… monotonic=…`) ⇒ `offset = realtime − monotonic`; phải in **định kỳ** vì mỗi lần NTP nhảy là offset đổi, neo cũ thành sai. **Lần sau hỏi:** đổi vỏ, giữ tầng vận dụng — *"thiết bị suspend 2 tiếng rồi wake; `steady_clock` đo được 3 giây còn thực tế trôi 2 tiếng — timeout của bạn hành xử ra sao, chọn `CLOCK_MONOTONIC` hay `CLOCK_BOOTTIME`?"* *(ghi chú cũ 2026-08-13:* Trắng ở dạng câu hỏi thật (*"chưa rõ về phần code hay kiến thức này"*); thu hẹp mới ra epoch 1970 + suy đúng **một** triệu chứng (NTP lùi ⇒ chờ thêm 1 giờ). Không biết `CLOCK_MONOTONIC`/`std::chrono::steady_clock`, không nêu được triệu chứng ngược (NTP tiến ⇒ **timeout ngay lập tức**), hỏi ngược *"NTP là gì?"*. Cần nắm: `REALTIME` = giờ theo lịch, **nhảy được** (NTP/admin/DST) ⇒ cấm dùng đo khoảng; quy tắc *"mấy giờ" → REALTIME · "bao lâu" → MONOTONIC*; C++ tương ứng `system_clock` vs **`steady_clock`**. Điều kiện JD (**không RTC**, NTP đồng bộ 30–60 s sau boot) làm cú nhảy gần như chắc chắn xảy ra. ⚖️ **Repo có lỗi thật:** bank ghi `→ file-io` mà file-io.md không có mục clock; nội dung nằm ở [TLPI cụm 03](../../15-book-summaries/the-linux-programming-interface/03-signals-and-timers.md). **Lần sau hỏi:** nén nền còn 1 checkpoint, vào thẳng vận dụng — *"log dùng timestamp REALTIME, đo trễ dùng MONOTONIC; giờ cần khớp một dòng log với một phép đo — làm sao?"* — ghi cả hai mốc một lần / `CLOCK_BOOTTIME`, bàn ca thiết bị suspend*) |
| BSP-021 | Linux có real-time không / `PREEMPT_RT` làm gì | bsp, melp | 1 | **2** | 🆕 2026-08-19. **Bỏ trắng nửa sau câu hỏi** — nêu được 3 nấc (phải probe mới ra nấc 3) nhưng **không trả lời một chữ nào** về *PREEMPT_RT làm gì*. ⚠️ **Lỗ hổng TRUY XUẤT**: hai gạch đầu dòng của đáp án (spinlock→`rt_mutex`, IRQ→kthread) **chính em vừa viết vào [driver-basics §6.6](../../05-drivers-device-tree/driver-basics.md) ngày 18/08**. Đổi nhãn câu hỏi từ *interrupt* sang *real-time* thì không lấy ra được. **Lần sau hỏi:** T1, góc *"PREEMPT_RT đổi cái gì trong kernel, và đánh đổi là gì?"* — không hỏi lại '3 nấc'. |
| BSP-029 | `initramfs` — thiết bị nhúng có LUÔN cần không | bsp, melp | 1 | **2** | 🆕 2026-08-19. **Đảo ngược luật và ngoại lệ**: đọc đúng đoạn *"khi nhúng vẫn nên dùng"* nhưng trình bày như luật chung; bank đánh ⭐ vào *"thiết bị nhúng thường KHÔNG cần"*. Cơ chế trả lời đúng hoàn toàn — chỉ sai ở chỗ **cấu trúc luật/ngoại lệ**. Probe một câu là tự sửa được. **Lần sau hỏi:** T1, góc *"khi nào em BỎ HẲN initramfs, và được lợi gì?"* — hỏi từ phía ngoại lệ để kiểm cấu trúc. |
| RES-012 | "resume thiếu Yocto / I2C-SPI / PCI-USB" — cách xử lý khoảng trống | resume, bsp | 2 | 2, **3** | 🔼 **1/2 lần ≥3** (2026-08-29, góc *"resume nay GHI Yocto/BitBake — kể em dùng thế nào"*). ✅ **Phần diễn đạt ĐÃ CHỮA:** đúng công thức 3 bước của bank — thừa nhận → bắc cầu (cross-compilation, CMake, port driver đa chipset) → nêu cụ thể bitbake/OE-Core/poky. 18/08 hỏng vì *mở đầu bằng phủ định, không bắc cầu*; nay bắc cầu được. 🔴 **NHƯNG lộ rủi ro NẶNG HƠN — khẳng định không đối chiếu được:** nói *"đã build minimal image chạy trên BBB, shell hoạt động qua serial"*, trong khi [plan §📍](../study-plans/datalogic-plan.md) ghi **8/8 lab BSP đều ⬜** và *"không có cáp USB–TTL 3.3V thì toàn bộ Thẻ A vô nghĩa"*. Đây đúng **bẫy ① của chính bank RES-012** (*"hỏi hai câu là lộ, mất niềm tin cả buổi"*). Interviewer đã hỏi 1 câu; câu thứ 2 (*"dòng log đầu tiên trên console?"*) sẽ quyết định. **Lần sau hỏi:** ⚠️ **hỏi thẳng câu thứ hai** — *"kể tôi nghe dòng đầu tiên U-Boot in ra, và bạn phải giữ nút nào lúc cấp nguồn"*. Trả lời được ⇒ 4đ + sửa tracking; không ⇒ kéo về 2 và **đổi hẳn câu trả lời sang bản an toàn** (bỏ mệnh đề BBB). |
| **RES-016** 🔴 | dòng AI trong resume — interviewer nghĩ gì bất lợi, chặn bằng câu nào | resume, behavioral | 1 | **0** | 🆕 2026-08-29. Trả lời *"không rõ"*. ⚠️ **Lỗ hổng NHẬN THỨC RỦI RO, không phải kiến thức** — không tự đọc được resume bằng mắt người tuyển. Điều bất lợi họ nghĩ: *"vậy phần nào là EM làm? bỏ AI đi thì còn gì?"* — với công ty tuyển kỹ sư C++ nhúng, dòng *"1 day → 1–2 hours"* đọc thành **người điều phối công cụ**, không phải người **giải được vấn đề**; càng nặng vì con số đó **ấn tượng hơn mọi con số khác** trong resume. 🔴 **Rủi ro này plan ĐÃ CẢNH BÁO từ 24/08** (BEH-005) mà vẫn lọt — vì cảnh báo ghi dưới dạng **một câu cụ thể** nên chỉ chặn đúng câu đó, không chặn được khi rủi ro **đổi vỏ** sang dòng resume. Câu chặn: *"AI giúp em đi nhanh ở phần **lặp lại**, nên phần em muốn đầu tư là chỗ **nó không làm thay được**"* + nối bằng chứng riêng (*"AI không đọc hộ em dmesg để thấy tiến trình RT chen vào giây thứ 4"*). **Lần sau hỏi:** T1 trước — *"đọc lại resume, chỉ ra dòng nào mở ra câu hỏi bất lợi nhất cho bạn"* (đo **nguyên tắc**, không đo thuộc lòng một câu). |
| **RES-001** ⚠️ | giới thiệu + project tâm đắc — **vấn đề trước, con số cuối** | resume, behavioral | 2 | 3, **3** | 🔁 **VÀO SỔ DÙ ĐƯỢC 3đ — ngoại lệ có chủ đích.** Luật §4 chỉ đưa câu ≤2 vào sổ; câu này vào vì **cùng một lỗi cấu trúc lặp LẦN THỨ BA** và sổ là nơi duy nhất bảo đảm nó bị hỏi lại. Chuỗi: kê đơn nguyên văn ở [log 18/08](sessions/2026-08-18--rapid--resume.md) → `BEH-001` hỏng y hệt 23/08 (*"bài tập 18/08 chưa làm"*) → **29/08 vẫn thiếu vế VẤN ĐỀ và vế CON SỐ**. ✅ **Đã sửa được một nửa:** nêu kết quả (chạy đơn lẻ + đồng bộ) **tự nguyện, không cần probe** — 18/08 phải probe mới ra. ❌ **Chưa sửa:** giữa câu vẫn là **danh sách công nghệ** (*vsync 60Hz, message queue*), đóng bằng *"hoàn thành tiến độ quan trọng"* — **không con số**; và cả câu là **một câu văn duy nhất** cho slot 30 giây. ⚠️ **KHÔNG phải lỗ hổng kiến thức** — nội dung nắm thừa. Là **chưa dựng khung + chưa nói to bấm giờ**; đọc thêm tài liệu không chữa được. **Lần sau hỏi:** yêu cầu **nói trong 45 giây, bấm giờ**, và chấm đúng hai tiêu chí — ① câu đầu tiên có phải **vấn đề** không (*"nhìn thành từng ô"*) ② câu cuối có **con số** không. Đạt cả hai 2 lần liên tiếp ⇒ gỡ. |
| CPP-054 | move ctor vs move assign (RAII fd, move-only) | cpp-system, modern-cpp | 1 | **2** | 🆕 Thêm 2026-08-10 (phát sinh từ follow-up CPP-045). Viết move ctor **đọc `fd_` chưa khởi tạo** (`if (fd_ != -1) close(fd_)` trong ctor) → UB, có thể `close()` fd của module khác; hỏi 2 lần vẫn trả lời *"fd_ mang giá trị từ constructor"* + *"hai chỗ cùng logic"*. Chưa nắm: **move ctor xây từ số 0 nên KHÔNG dọn**, move assign **phải dọn**. Thiếu `noexcept` (⟹ move-only không dùng được trong `vector`). Phụ: `<bits/stdc++.h>` trong header thư viện |

## 🔁 Lịch kiểm tra lại (retention) — hàng đợi, KHÔNG phải nghĩa địa

> **Vấn đề đã có:** câu gỡ khỏi sổ rơi vào bảng "Đã khắc phục" rồi **không cơ chế nào kéo về**. Trước đây có type `retention` lẽ ra làm việc đó, nhưng nó phải *"ưu tiên câu đã lâu chưa hỏi lại"* mà không nơi nào ghi ngày → thực tế bị bỏ qua (type đó đã bỏ 2026-08-18). Bảng này là nguồn rút cho **slot khởi động 🔁 đầu mỗi buổi**, thay cho việc quét `sessions/`.
>
> **Luật:** gỡ ở tuần N → kiểm tra lại **tuần N+2** (~2 tuần, đúng khoảng quên). Rải vào **slot khởi động 🔁 5–10′ đầu mỗi buổi** ([datalogic-plan](../study-plans/datalogic-plan.md) nguyên tắc ②), không dồn hết vào CN.
> **Hỏi theo config §6 luật ①:** nén phần nền còn 1 checkpoint, trọng số ở follow-up, **không lặp lại góc cũ**. Điểm **< 3 → kéo thẳng về bảng sổ yếu bên trên** (regression). Điểm ≥ 3 → dời lịch thêm 2 tuần hoặc coi như vững nếu hết plan.

> ⚠️ **Cột hạn dùng NGÀY, không dùng "Tuần N"** *(sửa 2026-08-17)*: plan đã bỏ khung Tuần 1–4, chuyển sang [lịch D1–D14](../study-plans/datalogic-plan.md). Nhãn tuần cũ thành mồ côi — giữ ngày tuyệt đối thì không phụ thuộc plan nào đang chạy.

| ID | Gỡ ngày | 🔁 Hạn kiểm tra lại | Góc đã dùng (**cấm lặp**) | Góc mới đề xuất | KQ |
|----|---------|---------------------|---------------------------|-----------------|-----|
| CPP-020 | 29/07 | **24–29/08** | ① Rule 0/3/5 là gì · ② class `unique_ptr`+`char*` lẫn lộn (10/08) · ③ dtor "vô hại" chỉ in log ⟹ mất move (13/08) | Cho class **có move tự viết** → hỏi copy bị gì; hoặc `= default` đặt ở header vs `.cpp` khác nhau ra sao | ✅ **4** (13/08) |
| CPP-029 | 03/08 | **24–29/08** | ① khác nhau thế nào · ② ctor `explicit` lọt qua emplace (10/08) · ③ `emplace_back(new W)` rò rỉ + khi nào emplace vô ích (13/08) | `emplace_back` vào `vector<T>` khi T có ctor **nhiều đối số** + reallocate giữa chừng → thứ tự thao tác | ✅ **3** (13/08) |
| CPP-009 | 07/08 | **04–09/09** | ① template ở header, vì sao · ② link error thật khi tách ra `.cpp` — đọc message, chẩn đoán (21/08) | Thư viện đang dùng explicit instantiation; giờ phải thêm `Stack<float>` mà **không được build lại `.so` đã giao khách** — còn đường nào? | ✅ **4** (21/08) |
| DP-002 | 07/08 | ~~17–23/08~~ | ① Singleton Meyers, lazy + thread-safe · ② Singleton trong `.so` — còn *một* instance không? (21/08) | — *(đã kéo về **bảng sổ yếu** bên trên; góc kế tiếp ghi ở đó)* | 🔻 **1** (21/08) |
| OS-003 | 07/08 | **04–09/09** | ① 4 điều kiện Coffman · ② 2 hàm lock ngược thứ tự → chỉ ra + **viết code sửa** (21/08) | `std::lock` phải thử lại nhiều lần — hai thread cứ nhả–thử–nhả–thử mãi thì gọi là hiện tượng gì, thực tế có xảy ra không? | ✅ **3** (21/08) — ⚠️ hụt **cơ chế bên trong**: nói *"đảm bảo thứ tự lock"* (sai, là **try-and-backoff**) và *"phá circular wait"* (sai, là **hold-and-wait**) |
| OS-007 | 07/08 | **04–09/09** | ① mutex vs semaphore, ownership, PI · ② tình huống RT 3 luồng H/M/L + binary semaphore → gọi tên + cơ chế cứu (21/08) | Bật `PTHREAD_PRIO_INHERIT` rồi mà H **vẫn** thỉnh thoảng trễ — còn nghi gì? (page fault trong critical section · L block trên I/O flash · inversion **kép** qua 2 khoá · kernel không phải PREEMPT_RT → nối [BSP-021](bank/bsp.md)) | ✅ **4** (21/08) |
| CPP-019 | 10/08 | **24–29/08** | 3 mức · release/acquire 3 ca · khi nào chọn acq/rel | **Đọc code**: đoạn producer/consumer thiếu nhãn ở một đầu → chỉ ra chỗ thiếu | ⬜ |
| CPP-024 | 10/08 | **24–29/08** | 3 tầng · cách sửa hot-reload config | Cho **backtrace crash trong `free()`** → chẩn đoán ngược về race tầng 2 | ⬜ |
| LNX-023 | 19/08 | **02–07/09** | ① `/proc`,`/sys` là gì (15/08) · ② rootfs tối giản không có `lsof` (16/08) · ③ vì sao `cat /proc/meminfo` luôn ra số mới (19/08) | **Đưa một đoạn code C đọc `/proc/self/status` rồi hỏi**: vì sao không `mmap` được file này, và vì sao `stat` báo size = 0? | ⏳ |
| CPP-032 | **21/08** | **04–09/09** | ① khác nhau thế nào · ② ctor `explicit` lọt qua emplace (10/08) · ③ hai chiều ngầm — lớp `Timeout` (13/08) · ④ ctor **nhiều đối số** + braced-init, dòng nào gãy (21/08) | `explicit` cho **conversion operator**: vì sao `explicit operator bool` mà `if (obj)` vẫn chạy (contextual conversion), trong khi `cout << obj` thì không | ⬜ |
| CPP-045 | 10/08 | **24–29/08** | link vs compile · viết lại class | **Code review**: đồng nghiệp đề xuất `private` không định nghĩa → phản biện | ⬜ |

Ký hiệu KQ: ⬜ chưa hỏi · ✅ ≥3 (vững, dời +2 tuần) · 🔻 <3 (**kéo về sổ yếu**)

---

## Đã khắc phục (lưu vết — gỡ khỏi bảng trên khi ≥3 điểm hai lần)

| ID | Câu | Ngày gỡ |
|----|-----|---------|
| CPP-020 | Rule of 0/3/5 | 2026-07-29 (điểm 2 → 4 → 4, đạt ≥3 hai lần liên tiếp) |
| CPP-029 | emplace_back vs push_back | 2026-08-03 (điểm 2 → 4 → 4, đạt ≥3 hai lần liên tiếp) |
| CPP-009 | template ở header — compile-time | 2026-08-07 (2 → 3 → **4**; nói thẳng *instantiation lúc compile* + linker error, trả lời được cả explicit instantiation) |
| DP-002 | Singleton hiện đại (Meyers static-local) | 2026-08-07 (2 → 3 → **4**; lazy + thread-safe **chỉ ở khởi tạo** + copy `=delete`) |
| OS-003 | deadlock — 4 điều kiện Coffman | 2026-08-07 (0 → 3 → **4**; đủ 4 điều kiện, nêu đúng "phải thoả ĐỒNG THỜI", lock ordering + try_lock timeout) |
| OS-007 | mutex vs semaphore | 2026-08-07 (2 → 4 → **4**; mục đích + ownership + priority inheritance kèm cơ chế) |
| CPP-019 | memory order — 3 mức + vì sao thận trọng | 2026-08-10 (2 → 3 → **3**; hỏi ở mức **thiết kế**: nêu đúng 3 điều kiện lập cặp acq/rel + ví dụ `relaxed` đủ dùng chuẩn (retry counter) + ranh giới *"biến atomic có dùng để suy luận về dữ liệu khác không"*. Còn thiếu: nói **"đo/profile rồi mới hạ"** như căn cứ quyết định) |
| CPP-024 | shared_ptr thread-safe (3 tầng) | 2026-08-10 (2 → 4 → **4**; tự dựng được mô hình **publish/RCU** cho hot-reload config: `atomic_load` giữ object cũ sống tới khi reader xong. Nhắc thêm: `atomic_load/store` **deprecated C++20, bỏ ở C++26**) |
| CPP-032 | explicit / implicit conversion | 2026-08-21 (gặp **4 lần**: 2 → 2 → 3 → **3**; góc cuối *ctor nhiều đối số + braced-init*: phân loại đúng copy-list-init vs direct-list-init. Còn hụt lúc gỡ: bỏ trống `return {…}` ở lượt đầu — **chính là cái giá đắt nhất** của quyết định — và chưa có **tiêu chí tổng quát** *khi nào để ngầm / khi nào explicit*) |
| CPP-045 | `=delete` vs private cũ | 2026-08-10 (2 → 4 → **3**; lý thuyết đạt — link error vs compile error khi gọi ngoài class, `= delete` để `public` cho message rõ. Phần code sinh ra câu mới [CPP-054](bank/cpp.md)) |

> **Ghi chú calibration (2026-08-07):** tên gọi nội bộ của compiler (vd *guard variable* ở DP-002) **không** tính là lỗ hổng — chỉ là "biết thì tốt". Tiêu chí giữ một câu trong sổ: **cơ chế giải thích được một lớp bug sẽ gặp trong công việc**, không phải thuật ngữ thuộc lòng.

> **Ghi chú calibration (2026-08-10) — CPP-019, tầng barrier ARM:** ứng viên phản hồi câu *"hạ seq_cst → acq/rel tiết kiệm gì ở tầng phần cứng ARM"* là quá sâu cho mid-level. ✅ **Đồng ý một phần** — tên lệnh (`stlr`/`ldar`/`dmb ish`) là kiến thức compiler-backend, **không chấm**, chỉ tính "biết thì tốt". ❌ **Nhưng giữ ý niệm**: *"acq/rel gần như free trên x86, có phí thật trên ARM"* vẫn phải biết, vì nó là lý do **tối ưu memory order đo trên máy dev x86 là vô nghĩa** — đúng lớp bug của JD ARM.
>
> **Thang chấm CPP-019 từ nay** (đã đồng bộ với [Trần độ sâu](config.md) — bổ sung 2026-08-10): ý (a) căn cứ hạ mức + ý (c) ranh giới `relaxed` = **3 điểm**; thêm ý niệm **x86 vs ARM** (đây là **T2** — nó đổi quyết định "đo ở đâu mới có nghĩa") = **4 điểm**. Tên lệnh barrier là **T3 — KHÔNG chấm** ở phiên mặc định, kể cả để nâng lên 4.
